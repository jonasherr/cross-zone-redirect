export default function CheckoutPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Checkout App — /checkout</h1>
      <p style={{ fontSize: '1.2rem', color: '#28a745' }}>
        If you can see this, the navigation to the checkout zone worked.
      </p>
      <p style={{ color: '#666' }}>
        This page is served by a separate Next.js application (different build ID).
      </p>
    </div>
  )
}
